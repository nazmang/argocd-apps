apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "anamnesticClaw.fullname" . }}
  labels:
    {{- include "anamnesticClaw.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "anamnesticClaw.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "anamnesticClaw.labels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      automountServiceAccountToken: false
      containers:
        - name: anamnestic-claw
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: DATABASE_PATH
              value: {{ .Values.env.databasePath | quote }}
            - name: SQLCIPHER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: SQLCIPHER_KEY
            - name: API_BEARER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secretName }}
                  key: API_BEARER_TOKEN
            {{- if .Values.documents.enabled }}
            - name: DOCUMENTS_S3_BUCKET
              value: {{ .Values.documents.bucket | quote }}
            - name: DOCUMENTS_S3_INSECURE
              value: {{ .Values.documents.insecure | quote }}
            - name: DOCUMENTS_S3_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: {{ include "anamnesticClaw.fullname" . }}-documents-minio
                  key: s3-endpoint
            - name: DOCUMENTS_S3_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "anamnesticClaw.fullname" . }}-documents-minio
                  key: s3-key
            - name: DOCUMENTS_S3_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "anamnesticClaw.fullname" . }}-documents-minio
                  key: s3-secret
            {{- end }}
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "anamnesticClaw.fullname" . }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
