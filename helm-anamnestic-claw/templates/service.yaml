apiVersion: v1
kind: Service
metadata:
  name: {{ include "anamnesticClaw.fullname" . }}
  labels:
    {{- include "anamnesticClaw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anamnesticClaw.selectorLabels" . | nindent 4 }}
