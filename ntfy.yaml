apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ntfy
  namespace: argocd
spec:
  project: local
  source:
    repoURL: https://github.com/nazmang/argocd-apps.git
    targetRevision: main
    path: helm-ntfy
    helm:
      releaseName: ntfy
      valueFiles:
        - values.yaml
        # Decrypted by the helm-secrets downloader plugin on argocd-repo-server.
        # This Application MUST stay single-source: ArgoCD requires $ref at the
        # start of a value-file string, which collides with the secrets:// prefix.
        - secrets://secrets.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: ntfy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  info:
    - name: upstream
      value: "https://ntfy.sh"
    - name: dockerhub
      value: "https://hub.docker.com/r/binwiederhier/ntfy"
