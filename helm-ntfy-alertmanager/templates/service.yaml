apiVersion: v1
kind: Service
metadata:
  name: {{ include "ntfy-alertmanager.fullname" . }}
  labels:
    {{- include "ntfy-alertmanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ntfy-alertmanager.selectorLabels" . | nindent 4 }}
